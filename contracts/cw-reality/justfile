default: build

build:
    cargo build --release --target wasm32-unknown-unknown --lib --locked

test:
    cargo test --locked

lint:
    cargo clippy --all-targets --locked -- -D warnings

schema:
    cargo run --example schema --locked

# Reproducible optimised wasm via the shared dao-contracts pattern.
# Pattern historically adapted from the dao-contracts optimizer recipe.
workspace-optimize:
    #!/bin/bash
    set -euo pipefail
    if [[ $(uname -m) == 'arm64' ]] || [[ $(uname -m) == 'aarch64' ]]; then \
        docker run --rm -v "$(pwd)":/code \
            --mount type=volume,source="$(basename "$(pwd)")_cache",target=/target \
            --mount type=volume,source=registry_cache,target=/usr/local/cargo/registry \
            --platform linux/arm64 \
            cosmwasm/optimizer-arm64:0.17.0; \
    elif [[ $(uname -m) == 'x86_64' ]]; then \
        docker run --rm -v "$(pwd)":/code \
            --mount type=volume,source="$(basename "$(pwd)")_cache",target=/target \
            --mount type=volume,source=registry_cache,target=/usr/local/cargo/registry \
            --platform linux/amd64 \
            cosmwasm/optimizer:0.17.0; \
    fi

# Quarantined mutation scripts

Files here can submit transactions to live networks. They are historical,
unsupported, excluded from validation, and must never be run by automation.
They intentionally require the operator to provide all binary/keyring paths by
environment variable; there are no repository-local defaults.
